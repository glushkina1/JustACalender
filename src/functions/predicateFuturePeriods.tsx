// import { useStore } from '../store/rootStore'

const predicateFuturePeriods = () => {
  // const store = useStore()
  // const { historyPeriods, cycleLength, periodLength } = store
  // const lastPeriod = Object.keys(historyPeriods).pop()
  // console.log('last Period:', lastPeriod)
}

export { predicateFuturePeriods }
